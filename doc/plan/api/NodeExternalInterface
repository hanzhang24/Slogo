public interface ExternalNode{
    // executes corresponding viewCommand associated with node
    // and returns a double
    public double execute(Model model)

    // attempts to construct a new node viewCommand tree from a string
    public static Node fromString(String input)

    // Set language. May only be called at the beginning of the program
    public void setLanguage(Document languageDoc)
}
Controller Internal Interface {

  // runs the command on the model
  private void runCommand(Node root) throws RuntimeError

  // adds a successful payload to the InstructionHistory
  public void addPayloadToHistory(ControllerPayload payload);
}
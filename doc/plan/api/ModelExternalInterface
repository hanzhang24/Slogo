Model External Interface {
  // runs a Payload from the Compiler and adds a return payload to the ViewTranslator queue as needed
  public void runPayload(Payload payload);

  // gets the most recent payload of commands as a List of Strings
  public List<String> getNewHistory();
}
Controller External Interface {
  // builds the instruction payload from the user input and calls the Model to run the payload
  public Payload buildPayload(String userInput);

  // gets the next instruction from the Payload
  public Instruction getNextInstruction()
  // skips the rest of a conditional statement and moves pointer to next valid instruction - applicable to ModelPayload only
  public void skipBranch();
}